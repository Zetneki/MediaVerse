const SERIES_SORTBY = {
  LAST_WATCHED: "last_watched",
  NAME: "name",
  STATUS: "status",
};

const VALID_SERIES_SORTBYS = Object.values(SERIES_SORTBY);

module.exports = {
  SERIES_SORTBY,
  VALID_SERIES_SORTBYS,
};
