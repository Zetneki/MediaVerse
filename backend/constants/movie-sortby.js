const MOVIE_SORTBY = {
  LAST_WATCHED: "last_watched",
  TITLE: "title",
  STATUS: "status",
};

const VALID_MOVIE_SORTBYS = Object.values(MOVIE_SORTBY);

module.exports = {
  MOVIE_SORTBY,
  VALID_MOVIE_SORTBYS,
};
